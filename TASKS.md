# Suvadu Tasks

The canonical task system is: **`tasks/atomic-task-list.md`**.

This root file is only a navigation pointer. Do not maintain a second task list here.

Future agents must use `tasks/atomic-task-list.md` for task IDs, phase definitions,
dependencies, acceptance criteria, evidence requirements, gate requirements, blockers, and
human-review checkpoints.

When task status changes, update the canonical task file plus `STATUS.md`, `CHECKPOINT.md`, and
any affected docs or notes.
